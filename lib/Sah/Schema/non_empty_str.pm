package Sah::Schema::non_empty_str;

use strict;

# AUTHORITY
# DATE
# DIST
# VERSION

our $schema = ['str1' => {
    summary => 'A non-empty string (length >= 1) (alias for str1)',
}];

1;
# ABSTRACT:

=head1 SEE ALSO

L<Sah::Schema::str1>
